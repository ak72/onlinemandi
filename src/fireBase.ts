export const a = 'a';

/* import {initializeApp} from 'firebase/app';
import {getAuth} from 'firebase/auth';
import {getDatabase} from 'firebase/database'; */

/* import firebase from 'firebase/compat/app';
import 'firebase/compat/auth'; */
// //App firebase configuration
/* const firebaseConfig = {
  apiKey: 'AIzaSyArOnpCIa3kh_1x34EqyVrjfoR6KwsfsjQ',
  authDomain: 'fins-eparent-default-rtdb.firebaseio.com',
  databaseURL: 'https://freshonlinemandi-default-rtdb.firebaseio.com',
  projectId: 'freshonlinemandi',
  storageBucket: 'freshonlinemandi.appspot.com',
  messagingSenderId: '1070022446261',
}; */

// // Initialize Firebase
// let fbApp = initializeApp(firebaseConfig);

// // //get authentication
// export const fbAuth = getAuth(fbApp);

// // // export database so it can be used elsewhere
// export const fbDatabase = getDatabase(fbApp);

/* let fbApp;

if (firebase.apps.length === 0) {
  fbApp = firebase.initializeApp(firebaseConfig);
} else {
  fbApp = firebase.app();
}

const db = fbApp.database();
const fbAuth = firebase.auth();
"firebase": "9.7.0"9.6.11
export {db, fbAuth};
 */
